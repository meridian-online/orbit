# Pre-Merge Review

**Date:** 2026-05-08
**Reviewer:** Context-separated agent (fresh session)
**Branch:** drive/exec-comm-wires
**Spec:** 2026-05-08-executive-communication-wires
**Verdict:** APPROVE

---

## Test Results

| Check | Result | Details |
|-------|--------|---------|
| Test suite (plugin) | PASS | 2/2 plugin scripts pass (`test-gate-ac-verification.sh`, `test-promote-gate-propagation.sh`) |
| `orbit verify` | PASS | reports `clean` |
| AC coverage | 0/9 | See report below — every AC is doc/wiring, not code; no test gap |

## AC Coverage Report

| AC | Status | Test(s) |
|----|--------|---------|
| ac-01 | — | doc deliverable (STYLE.md content) — verified by file inspection |
| ac-02 | — | doc deliverable (CLAUDE.md import line) — verified by grep |
| ac-03 | — | doc deliverable (design SKILL.md citation) — verified by grep |
| ac-04 | — | doc deliverable (review-spec SKILL.md citation) — verified by grep |
| ac-05 | — | doc deliverable (review-pr SKILL.md citation) — verified by grep |
| ac-06 | — | process AC (`@`-resolution noted in spec notes) — verified by reading notes |
| ac-07 | — | edit AC (card 0026 references audited) — verified by file inspection |
| ac-08 | — | gate AC (`orbit verify` + `spec close`) — half-met; see Findings |
| ac-09 | — | runtime spot-check AC — partially met; see Findings |

Every AC in this spec describes a documentation/wiring deliverable or a process gate, not a code change. The unit-test pattern (`fn ac<NN>` / `def test_ac<NN>`) does not apply: there is no behavioural surface to assert against, only files to inspect and grep. This is consistent with the spec's nature as a directive-only wiring change.

## Findings

### [INFO] AC-01 — STYLE.md content
**Category:** doc-verification
**Description:** `.orbit/STYLE.md` exists at the repo root (38 lines) and contains every named element: TL;DR / Recommendation / Why / Detail / Confidence skeleton, recommendation discipline paragraph, all seven anti-patterns by name (Lede-burying, Hedge-stacking, Pre-emptive detail dump, Menu-presenting, Undefined jargon, Apologetic preambles, Restating the question), the four-row response-variant table (factual/status/decision/research), and the British-English tone contract.
**Evidence:** `/home/hugh/github/hughcameron/orbit/.orbit/STYLE.md` lines 1–38; grep confirms all seven anti-pattern names present.
**Recommendation:** Met.

### [INFO] AC-02 — CLAUDE.md @-import
**Category:** doc-verification
**Description:** `CLAUDE.md` line 7 carries `@.orbit/STYLE.md` early in the file (above the four-pillars framing at line 48). The four-pillars framing is preserved.
**Evidence:** `CLAUDE.md:5–7` for the citation+import; `CLAUDE.md:48–57` for the preserved four-pillars block.
**Recommendation:** Met.

### [INFO] AC-03 / AC-04 / AC-05 — Skill SKILL.md citations
**Category:** doc-verification
**Description:** All three skills carry the canonical belt-and-braces pattern — a one-line prose marker citing card 0026 by path, immediately followed by `@.orbit/STYLE.md`. Pattern is identical across the three files (no mix-and-match).
**Evidence:**
- `plugins/orb/skills/design/SKILL.md:10–12`
- `plugins/orb/skills/review-spec/SKILL.md:14–16`
- `plugins/orb/skills/review-pr/SKILL.md:12–14`
**Recommendation:** All three met.

### [INFO] AC-06 — `@`-import resolution recorded
**Category:** process-verification
**Description:** Spec notes contain two entries: the first records the canonical pattern decision (single belt-and-braces pattern across all three skills, citing the Anthropic best-practices URL for CLAUDE.md `@` resolution and noting plugin SKILL.md `@` semantics are not explicitly documented). The second records that an in-session subagent recall test is the wrong instrument because forks see a cached pre-edit CLAUDE.md.
**Evidence:** `.orbit/specs/2026-05-08-executive-communication-wires.notes.jsonl` (2 entries); also captured as a memory at `.orbit/memories/claude-code-at-import-fork-behaviour.yaml`.
**Recommendation:** Met. The decision is documented and a reusable memory is filed for future sessions.

### [INFO] AC-07 — Card 0026 stale references audited
**Category:** doc-verification
**Description:** The deleted memo reference is annotated as historical (line 59 of card 0026: "historical: 2026-05-06 distillation memo (executive-communication-framework) — deleted after promotion per orbit convention"). The `ops decision 0029` line (line 60) was kept verbatim. The reference is benign — it cites a precursor decision by id and one-word topic ("recommendation discipline"); no proprietary content is exposed by leaving it.
**Evidence:** `.orbit/cards/0026-executive-communication.yaml:59–60`. Memo dir `ls` shows `2026-05-06-executive-communication-framework.md` is indeed gone.
**Recommendation:** Met.

### [LOW] AC-08 — `orbit spec close` runs at drive completion, not pre-merge
**Category:** scheduling-mismatch (not a defect)
**Description:** `orbit verify` is clean. `orbit spec close 2026-05-08-executive-communication-wires` has *not* yet run — the spec's status is `open` and card 0026's `specs:` array is still `[]`. This is the standard drive workflow: `spec close` runs after `review-pr` returns APPROVE, not before. The implementer-checked `[x]` on ac-08 is therefore a forward-looking claim, not a present fact, but it matches drive's documented sequencing (drive SKILL.md instructs `orbit spec note ... && orbit spec close` after the PR-review step).
**Evidence:** `orbit --json card show 0026-executive-communication` → `specs: []`. `orbit spec list` shows the spec as `open`. Drive SKILL.md shows `spec close` lives after the review-pr stage.
**Recommendation:** No change required pre-merge. Drive must run `orbit spec note ... && orbit spec close` before declaring the cycle done; if drive fails to close transactionally, card 0026's `specs:` will stay `[]` and choice 0019's audit signal will not flip. Worth a post-merge spot-check that `orbit --json card show 0026-executive-communication | jq .data.result.card.specs` is non-empty.

### [LOW] AC-09 — Runtime load verification deferred to next session
**Category:** environment-mismatch (acknowledged in notes)
**Description:** Spec note 2 records the empirical finding that `@`-imports and CLAUDE.md edits do *not* reach in-session subagent forks (cached pre-edit snapshot). The `cat CLAUDE.md` half of ac-09 is verifiable and is met (line 7 carries `@.orbit/STYLE.md`). The "agent session can recall STYLE.md content without re-reading card 0026" half cannot be tested in the same session that performed the edit; it requires a fresh session post-merge.
**Evidence:** `CLAUDE.md:7` shows the import line. `.orbit/memories/claude-code-at-import-fork-behaviour.yaml` documents the in-session caching behaviour.
**Recommendation:** Acceptable for merge. The wires are correctly installed (file inspection confirms); the runtime recall test belongs to the first new session opened in this repo after merge. Note the implementer has already filed a reusable memory so future sessions don't re-discover this.

### [INFO] Choice 0019 conformance
**Category:** decision-respect
**Description:** This spec is the canonical worked example of choice 0019 (cards declare framework wires; aspirational cards don't pass review). Card 0026 carries the "Wired into the framework" gate scenario; this spec ships the wires (CLAUDE.md preamble, three skill SKILL.md citations) and links itself to the card via `cards: [0026-executive-communication]`. Once `spec close` runs, card 0026's `specs:` array flips from `[]` to non-empty and the audit signal moves from aspirational to wired — exactly the operationalisation 0019 specifies.
**Evidence:** `.orbit/choices/0019-cards-declare-framework-wires.yaml` (lines 28–36 enumerate the wire taxonomy; 65 names this card as the canonical instance). Spec's `cards:` field links 0026.
**Recommendation:** Conforming.

### [INFO] Adjacent finding — `promote.sh` is stale vs orbit-state
**Category:** observation (not a defect of this PR)
**Description:** The implementer filed a memory (`promote-sh-stale-vs-orbit-state.yaml`) noting that `plugins/orb/scripts/promote.sh` still uses the bd CLI and bd metadata fields, predating orbit-state v0.1. Drive worked around this manually for this spec. Orthogonal to this PR; future drives may wedge until ported.
**Evidence:** `.orbit/memories/promote-sh-stale-vs-orbit-state.yaml`.
**Recommendation:** Out of scope for this review. The memory captures the finding for the next session that touches drive's promote stage.

---

## Honest Assessment

The wires are correctly installed and visible to fresh inspection. STYLE.md exists with every named element from ac-01; CLAUDE.md carries the `@`-import in a load-bearing position above the four-pillars framing; all three skill SKILL.md files carry the identical belt-and-braces pattern (prose citation of card 0026 + `@.orbit/STYLE.md` import). Card 0026's stale memo reference is annotated as historical; the `ops decision 0029` pointer is benign and stays. `orbit verify` is clean. Choice 0019's conformance test passes structurally — once `orbit spec close` runs, card 0026's `specs:` array flips from `[]` to non-empty and the aspirational-card audit signal flips with it.

Two ACs land at "met but with caveat" rather than "met flat":

ac-08 — `orbit spec close` has not yet run. This is by drive design (close is the last step after review-pr APPROVE, not before), and the spec.yaml + `cards:` link is staged correctly such that close will fire transactionally. The pre-merge mechanical check it really requires is `orbit verify: clean`, which passes; the post-close check belongs to drive's wrap-up, not this review.

ac-09 — the second clause ("an agent session can recall STYLE.md content without re-reading card 0026") cannot be tested in the same session that did the edits, because Claude Code subagent forks see a cached pre-edit CLAUDE.md. The implementer captured this empirically in a memory and a spec note. The first clause (`cat CLAUDE.md` shows the import line) is met. Full runtime verification belongs to the first fresh session post-merge — a worthwhile sanity check, but not a merge blocker, because the wires are demonstrably installed at the file layer.

Every AC is a documentation/wiring deliverable or a process gate. No code surface, no behavioural test to write. The "0/9 AC test coverage" line in the table reflects the spec's nature, not a test gap. The two existing plugin test scripts pass and remain orthogonal to this change.

The spec is an honest first instance of choice 0019 in production. It demonstrates the wires-in-cards discipline by example, files reusable memories about Claude Code's session-cache behaviour and promote.sh's staleness for future agents, and ships the wires that flip card 0026 from aspirational to wired. Recommend APPROVE; recommend drive's wrap-up step run `orbit spec close` and then a fresh-session spot-check confirms the import is loaded.
